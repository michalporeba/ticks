name: ticks
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  bloc: ^8.1.1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_localizations:
    sdk: flutter
  google_fonts: ^6.2.1
  intl: ^0.19.0
  local_ticks_api:
    path: packages/local_ticks_api
  meta: ^1.7.0
  shared_preferences: ^2.2.2
  ticks_api:
    path: packages/ticks_api
  ticks_repository:
    path: packages/ticks_repository
  ticks_domain:
    path: packages/ticks_domain

dev_dependencies:
  bloc_test: ^9.1.0
  flutter_test:
    sdk: flutter
  mockingjay: ^0.5.0
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
  generate: true