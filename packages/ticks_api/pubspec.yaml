name: ticks_api
description: The interfaces and model for the API providing access to ticks
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.3
  json_annotation: ^4.8.1
  meta: ^1.7.0
  uuid: ^4.0.0

dev_dependencies:
  build_runner: ^2.2.0
  json_serializable: ^6.3.1
  test: ^1.21.4
  very_good_analysis: ^5.1.0
